<UserControl x:Class="VGCatalog.WPF.Components.SearchingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VGCatalog.WPF.Components"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Visibility="{Binding IsSearching, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#1F1F1F"
                BorderBrush="#1F1F1F"
                BorderThickness="5"
                CornerRadius="22"
                Opacity="0.75" />
        <Border Grid.Row="0"
                Width="350"
                Height="100"
                Margin="5"
                Background="#121212"
                BorderBrush="Gray"
                BorderThickness="2"
                CornerRadius="30">
            <!--<Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0.0" Color="#FBB03B" />
                    <GradientStop Offset="0.5" Color="#D4145A" />
                    <GradientStop Offset="1.0" Color="#FBB03B" />
                </LinearGradientBrush>
            </Border.BorderBrush>-->
            <StackPanel Width="290"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MouseDown="StackPanel_MouseDown"
                        Opacity="1">

                <TextBlock HorizontalAlignment="Center"
                           FontSize="30"
                           Foreground="LightGray"
                           Text="Searching..." />

                <Line Style="{StaticResource SearchingLineAnimation}" Visibility="{Binding IsSearching, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
